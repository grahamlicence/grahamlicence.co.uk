{{>site-header}}


    <main class="main-content {{ this.page.class }}">
    
        <nav class="cartoons-list">
            <ul class="sub-nav">

            {{#is this.page.group 'birdCartoon' }} 

                {{#each pages }}

                    {{#is data.group 'birdCartoon' }} 

                        {{#is data.title 'Bird cartoons' }}
                        {{else}}
                            <li {{#if isCurrentPage}} class="active"{{/if}}>
                                {{#if production}}
                                <a href="{{ replaceStr this.dest 'dist' '' }}">{{ data.title }}</a>
                                {{else}}
                                <a href="{{ replaceStr this.dest 'dev' '' }}">{{ data.title }}</a>
                                {{/if}}
                            </li>
                        {{/is}}

                    {{/is}}

                {{/each}}

            {{/is}}

            {{#is this.page.group 'cartoons' }} 

                {{#each pages }}

                    {{#is data.group 'cartoons' }} 

                        {{#is data.title 'Cartoons' }}
                        {{else}}
                            <li {{#if isCurrentPage}} class="active"{{/if}}>
                                {{#if production}}
                                <a href="{{ replaceStr this.dest 'dist' '' }}">{{ data.title }}</a>
                                {{else}}
                                <a href="{{ replaceStr this.dest 'dev' '' }}">{{ data.title }}</a>
                                {{/if}}
                            </li>
                        {{/is}}

                    {{/is}}

                {{/each}}

            {{/is}}

            {{#is this.page.group 'insomniaCartoons' }} 

                {{#each pages }}

                    {{#is data.group 'insomniaCartoons' }} 

                        {{#is data.title 'Insomnia cartoons' }}
                        {{else}}
                            <li {{#if isCurrentPage}} class="active"{{/if}}>
                                {{#if production}}
                                <a href="{{ replaceStr this.dest 'dist' '' }}">{{ data.title }}</a>
                                {{else}}
                                <a href="{{ replaceStr this.dest 'dev' '' }}">{{ data.title }}</a>
                                {{/if}}
                            </li>
                        {{/is}}

                    {{/is}}

                {{/each}}

            {{/is}}

            </ul>
        </nav>
        
        <h1>{{ this.page.cartoonTitle }}</h1>
        
        {{!-- <img src="/assets/images/{{ this.page.cartoonImage }}" alt="{{ this.page.cartoonAlt }}" /> --}}
        
        {{> body}} 

    </main>
    
    {{>sidebar}}

{{>site-footer}}